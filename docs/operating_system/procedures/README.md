# Procedures

Procedures turn Personal OS rules into repeatable actions. Add a procedure
only when a workflow is used more than once or needs explicit evidence.

Runtime generation starts with:

```powershell
python scripts/generate_openclaw_surface.py --check
```

